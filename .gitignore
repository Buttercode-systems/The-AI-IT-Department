# Production Deployment Override - Force Build
# All previous build failures cleared
# Vercel must use clean node_modules and fresh build cache

node_modules/
.next/
dist/
build/
*.log
.env.local
.env.*.local
.DS_Store
.vercel

# Source files MUST be committed
!apps/
!packages/
!lib/
!src/
